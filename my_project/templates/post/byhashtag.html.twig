{% extends 'base.html.twig' %}
{% block body %}
    <div class="archive-body">
        <button onclick="goBack()" class="back-button-author">atgal</button>
        <br>
        <br>
        <br>
        <div style="font-size: 20px">postai su <span style="font-weight: bold">#{{ hashtag }}</span>:</div>
        <br>
        {% for post in posts %}
         <div class="post-by-hashtag-list">
            <a href="{{ path('post_show',{'id': post.id}) }}">{{ post.title }}</a>
        </div>
            <br>
            <br>
        {% endfor %}
    </div>
     <script>
         function goBack() {
             window.history.back();
         }
    </script>

{% endblock %}