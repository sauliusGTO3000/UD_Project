{% extends 'base.html.twig' %}
{% block body %}
    <div class="archive-body">
    <button onclick="goBack()" class="back-button-author">atgal</button>
    <br>
    <br>
    <br>
        <div>postai su #{{ hashtag }}</div>
        <br>
        {% for post in posts %}
         <div class="post-list">
            <a href="{{ path('post_show',{'id': post.id}) }}">{{ post.title }}</a>
        <div>
        {% endfor %}

    </div>
             <script>
                 function goBack() {
                     window.history.back();
                 }
                 </script>
{% endblock %}