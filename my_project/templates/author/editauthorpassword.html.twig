{% extends 'base.html.twig' %}

{% block body %}
    <div class="author-show-body">
    {{ form_start(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
    {{ form_end(form) }}
    </div>
{% endblock %}