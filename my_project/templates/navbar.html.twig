{% block navbar %}

    <div class="navbar-block">
        <div class="logo-block"><a href="{{ path('homepage') }}"><img src="/styleelements/logo-small.png" id="logosmallnavbar"></a></div>

        <div class="menu-block">
            <ul id="menu-list">
                <li><a href="{{ path('homepage') }}#mainHomepage">dabar</a></li>
                <li><a href="{{ path('archive') }}">archyvas</a></li>
                <li><a href="{{ path('kitidalykai') }}">kiti dalykai</a></li>
                {% if app.user %}
                    <li><a href="{{ path('autoriausKampelis') }}">autoriaus kampelis</a></li>
                {%  endif %}

            </ul>

        </div>

        <div class="ending-block">
            <button>meniu</button>
        </div>
    </div>
    <div class="dropdown-menu" id="dropdown-menu" hidden >
        <a href="{{ path('homepage') }}">dabar</a>
        <hr>
        <a href="{{ path('archive') }}">archyvas</a>
        <hr>
        <a href="{{ path('kitidalykai') }}">kiti dalykai</a>
        {% if app.user %}
           <a href="{{ path('autoriausKampelis') }}">autoriaus kampelis</a>

        {%  endif %}
    </div>


{% endblock %}